// Basic operators, maths

// Terms: "unary", "binary", "operand"

// 1. An operand - is what operators are appled to. For instance, in the multiplication
// of 5*2 there are two operands: the left operand is 5 and the right operand is 2.
// Sometimes, people call these "arguments" instead of "operands".

// 2. An operator is unary if it has a single operand. For example the unary negation
// - reverses the sign of a number:
let x = 1;
x = -1;
console.log(x); // =1, unary negation was applied

// 3. An operator is binary if it has two operands. The same minus exists in binary
// form as well:
let j = 1,
  k = 3;
alert(y - x);

// Maths
// The following math operations are supported:
// - Addition +
// - Subtraction -
// - Multiplication *
// - Division /
// - Remainder %
// - Exponentiation **

// Bitwise operators
// - AND (&)
// - OR (|)
// - XOR (^)
// - NOT (!)
// - LEFT SHIfT (<<)
// - RIGHT SHIfT (>>)
// - ZERO-FILL RIGHT SHIFT (>>>)
