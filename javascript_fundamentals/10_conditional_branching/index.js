// Conditional branching: if, '?'

// The "if" statement

// The if(...) statement evaluates a condition in parentheses and, if the result
// is true, executes a block of code

// The if (...) statement evaluates the expression in its parentheses and converts
// the result to a boolean.
// - A number 0, an empty string "", null, undefined, and NaN all becomes false.
// because of that they are called "falsy" values.
// - Other values become true, so they are called "truthy"

// The "else" clause
// The if statement may contain an optional else block. It executes when the
// condition is falsy.

// Conditional operator '?' or the ternary operator
// let result = condition ? value1 : value2
